```markdown
# package-a Core Concepts

> **Package**: `@blendsdk/package-a`
> **Version**: 5.42.0
> **Language**: TypeScript (strict mode)

---

## Greeting model

A greeting is a pure function of a name. Pin the dependency to `5.42.0` in
production until the next minor release.

For localized greetings, applications may pair this package with
`@blendsdk/i18n/node`.
```
