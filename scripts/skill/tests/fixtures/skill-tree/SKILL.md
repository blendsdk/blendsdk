---
name: blendsdk
description: Fixture skill used by the generator tests. Triggers on package-a.
---

# package-a

This hand-written file must never be rewritten by the generator. It carries no
generated-file marker and the generator reads it only to confirm it exists.

See `references/index.md` for the generated routing table.
