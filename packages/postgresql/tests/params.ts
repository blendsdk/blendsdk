import { PostgreSQLConfig } from '../src/database.js';

export const connectionConfig: PostgreSQLConfig = {
  host: 'localhost',
  port: 5599,
  database: 'testdb',
  pass: 'testdb',
  user: 'testdb',
};
