services:
  redis-test:
    image: redis:7-alpine
    container_name: webafx-redis-test-ci
    ports:
      - "6379:6379"
    command: redis-server --appendonly no
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
