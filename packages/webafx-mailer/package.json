{
  "name": "@blendsdk/webafx-mailer",
  "version": "5.53.0",
  "description": "Email sending plugin for WebAFX with SMTP and In-Memory backends",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/blendsdk/blendsdk.git",
    "directory": "packages/webafx-mailer"
  },
  "scripts": {
    "build": "tsc",
    "test:fast": "vitest run --reporter=verbose",
    "test:watch": "vitest watch --reporter=verbose",
    "clean": "rm -fR ./dist",
    "test:coverage": "vitest run --coverage",
    "dev": "tsc --watch",
    "docker:up": "docker-compose -p webafx-mailer -f ./docker/docker-compose${MODE}.yml up -d && sleep 5",
    "docker:down": "docker-compose -p webafx-mailer -f ./docker/docker-compose${MODE}.yml down -v --remove-orphans",
    "docker:logs": "docker-compose -p webafx-mailer -f ./docker/docker-compose${MODE}.yml logs mailpit",
    "test": "yarn docker:down && yarn docker:up && vitest run --reporter=verbose && yarn docker:down"
  },
  "dependencies": {
    "nodemailer": "^9.0.3"
  },
  "devDependencies": {
    "@blendsdk/webafx": "^5.53.0",
    "@types/node": "^26.1.2",
    "@types/nodemailer": "^8.0.1",
    "@vitest/coverage-v8": "^4.1.10",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  },
  "peerDependencies": {
    "@blendsdk/webafx": "^5.53.0"
  },
  "license": "MIT",
  "peerDependenciesMeta": {
    "@blendsdk/webafx": {
      "optional": true
    }
  },
  "private": true
}
