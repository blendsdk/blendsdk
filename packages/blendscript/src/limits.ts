/** Maximum expression source length in UTF-16 code units. */
export const MAX_SOURCE_LENGTH = 16_384;
/** Maximum number of non-EOF tokens. */
export const MAX_TOKEN_COUNT = 4_096;
/** Maximum decoded string literal length in UTF-16 code units. */
export const MAX_STRING_LENGTH = 4_096;
/** Maximum decoded source field name and schema key length. */
export const MAX_FIELD_NAME_LENGTH = 256;
/** Maximum schema field count. */
export const MAX_SCHEMA_FIELDS = 1_024;
/** Maximum parser nesting depth. */
export const MAX_NESTING_DEPTH = 64;
/** Maximum semantic diagnostics returned by one call. */
export const MAX_SEMANTIC_DIAGNOSTICS = 20;
/** Maximum work charged by one evaluation. */
export const MAX_EVALUATION_STEPS = 10_000;
/** Maximum source excerpt length in UTF-16 code units. */
export const MAX_EXCERPT_LENGTH = 120;
